import { Andika } from 'next/font/google';

export const andika = Andika({
    weight: ['400', '700'],  // Specify the weights you need
    subsets: ['latin'],
    display: 'swap',
})   

// console.log('wee', andika)